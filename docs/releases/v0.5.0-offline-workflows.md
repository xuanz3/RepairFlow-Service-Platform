# v0.5.0-offline-workflows

Pre-release checkpoint for durable offline workflows.

Includes stable operation envelopes, desktop/mobile SQLite outboxes, bounded retries, idempotent service replay, cursor-based delta pull, tombstones, explicit conflicts and restart recovery tests.

The release checkpoint is the controlled-failure verification commit. It is not a final packaged application release.
